/**
 * Domain Entities
 * 
 * Entities are objects that have a distinct identity that runs through time
 * and different representations. They are defined by their identity, not their attributes.
 */

export { User, type UserProps } from './User';
export { Product, type ProductProps } from './Product';
export { Order, OrderItem, type OrderProps, type OrderItemProps } from './Order';
export { Category, type CategoryProps } from './Category';
